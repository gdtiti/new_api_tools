# 模式沉淀

最后更新：2026-03-18

## 容器启动自检模式

适用场景：

- 外部平台通过单端口探活容器。
- 容器内部存在反向代理和真实业务端口两层链路。

模式要点：

1. 启动前先渲染模板配置，避免依赖运行中热改配置。
2. 启动后先做本地 readiness 检查，再交给平台探活。
3. 健康检查要分层输出诊断，明确区分代理层与业务层故障。
4. 失败时打印最终渲染配置，减少平台环境排障成本。

在本项目中的落点：

- `docker/entrypoint.sh`
- `docker/healthcheck.sh`
- `frontend/nginx.conf`
